#include <stdio.h>

int main() {
    int count = 0;

    for (int i = 0; i <= 127; i++) {
        printf("%3d: '%c'   ", i, (char)i);
        
        count++;
        if (count == 10) {
            printf("\n");
            count = 0;
        }
    }

    return 0;
}
